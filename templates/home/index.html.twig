{% extends 'base.html.twig' %}

{% block title %}Test Symfony Le Comptoir des langues{% endblock %}

{% block body %}
<h1>Test Symfony</h1>

    <div>
        <a href="{{ path('livre_index') }}">Liste des livres</a>
    </div>
    <div>
        <a href="{{ path('categorie_index') }}">Liste des catégories</a>
    </div>
        <a href="{{ path('auteur_index') }}">Liste des auteurs</a>
    </div>
{% endblock %}
